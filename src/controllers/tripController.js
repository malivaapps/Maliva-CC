
const generatePlan = (req, res) => {
     res.status(200).json({data : "SOLO, JOGJA"})
}

const saveTripPlan = (req, res) => {
     res.status(200).json({data : "SOLO, JOGJA"})
}

module.exports = {
     generatePlan,
     saveTripPlan
}